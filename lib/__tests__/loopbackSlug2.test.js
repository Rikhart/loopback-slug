const assert = require('assert');
const loopbackSlug2 = require('../index.js');

describe('loopbackSlug2', () => {
  it('has a test', () => {
    assert(false, 'loopbackSlug2 should have a test');
  });
});
